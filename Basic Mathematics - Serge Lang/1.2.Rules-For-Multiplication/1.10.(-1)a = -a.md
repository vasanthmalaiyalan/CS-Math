இதுதான் ஒரு அடிப்படை Definition + Theorem-இன் விளைவு.

நினைவிருக்கிறதா? புத்தகத்தில் N7:

```text id="m12"
(-1)a = -a
```

என்று நிரூபித்தார்கள்.

---

அந்த proof:

```text id="m13"
(-1)a + a
=
(-1)a + 1a
=
(-1+1)a
=
0a
=
0
```

---

அதனால்:

```text id="m14"
(-1)a
```

என்பது

```text id="m15"
a
```

-ன் additive inverse.

---

Additive inverse என்பது என்ன?

```text id="m16"
a + (-a) = 0
```

ஆகும் எண்.

---

ஆகவே:

```text id="m17"
(-1)a = -a
```

---

இப்போது உன் கேள்வி:

> ஏன்
>
> ```text
> -a = (-1)a
> ```
>
> என்று எழுதலாம்?

---

ஏனெனில் N7 ஏற்கனவே சொல்கிறது:

```text id="m18"
(-1)a = -a
```

---

கணிதத்தில்:

```text id="m19"
x = y
```

என்றால்

```text id="m20"
y = x
```

என்றும் உண்மை.

---

அதனால்:

```text id="m21"
(-1)a = -a
```

இருந்தால்

```text id="m22"
-a = (-1)a
```

என்றும் உண்மை.

---

### எண் உதாரணம்

```text id="m23"
a = 5
```

---

அப்போது:

```text id="m24"
-a
=
-5
```

---

மற்றொரு பக்கம்:

```text id="m25"
(-1)a
=
(-1)×5
=
-5
```

---

இரண்டும் ஒரே எண்.

அதனால்:

```text id="m26"
-5 = (-1)×5
```

---

பொதுவாக:

```text id="m27"
-a = (-1)a
```

---

### ஆழமான பார்வை

Addition chapter-ல்:

```text id="m28"
-a
```

என்பது

> "a-ன் additive inverse"

என்று வரையறுக்கப்பட்டது.

---

Multiplication chapter-ல் அவர்கள் நிரூபிக்கிறார்கள்:

```text id="m29"
(-1)a
```

என்பதும்

> "a-ன் additive inverse"

என்று.

---

ஒரே எண்ணுக்கு additive inverse ஒன்று மட்டுமே இருக்கும்.

அதனால்:

```text id="m30"
-a
=
(-1)a
```

---

இதுதான் உண்மையான கணித காரணம். 🎯

அதாவது:

```text id="m31"
-a
```

என்பது ஆரம்பத்தில் "minus sign போட்ட notation".

பிறகு multiplication rules மூலம்,

```text id="m32"
(-1)a
```

அதே வேலையைச் செய்கிறது என்று நிரூபிக்கிறோம்.

அதன்பிறகுதான் Algebra-வில் எல்லோரும்

```text id="m33"
-a
```

ஐ

```text id="m34"
(-1)a
```

என்று freely மாற்றி பயன்படுத்த ஆரம்பிக்கிறார்கள்.


-------------------------------------------------

``(-1)a + a
=
(-1)a + 1a
=
(-1+1)a
=
0a
=
0`இந்த ஆதாரத்தில் எதற்காக +a என்று கூடுதலாக பயன்படுத்தினாய்?`

இது மிக நல்ல கேள்வி. 🎯

நீ கேட்பது:

> ஏன் proof-ல் திடீரென்று
>
> ```text
> (-1)a + a
> ```
>
> என்று எடுத்தார்கள்?
>
> `+a` எங்கிருந்து வந்தது?

---

காரணம்:

புத்தகம் நிரூபிக்க வேண்டியது:

```text id="k1"
(-1)a = -a
```

---

ஆனால் `-a` என்பதன் definition என்ன?

Addition chapter-ல்:

```text id="k2"
a + (-a) = 0
```

---

அதாவது:

> "`a`-வுடன் கூட்டினால் 0 ஆகும் எண்"

அதுதான் `-a`.

---

எனவே `(-1)a` உண்மையில் `-a` தானா என்பதை தெரிந்துகொள்ள,

புத்தகம் check செய்கிறது:

```text id="k3"
(-1)a + a
```

எவ்வளவு?

---

அது 0 வந்தால்,

```text id="k4"
(-1)a
```

என்பதும்

```text id="k5"
-a
```

போலவே additive inverse ஆகிவிடும்.

---

உதாரணம்:

நீ யாரையாவது பற்றி சொல்கிறாய்:

> "இந்த மனிதர் தான் ராமன்."

நான் கேட்கிறேன்:

> "எப்படி உறுதியாக சொல்கிறாய்?"

நீ சொல்கிறாய்:

> "ராமனின் அனைத்து அடையாளங்களும் இவரிடம் இருக்கின்றன."

---

அதே போல.

`-a`-வின் அடையாளம்:

```text id="k6"
a + (-a) = 0
```

---

அதனால் `(-1)a`-வுக்கு அந்த அடையாளம் இருக்கிறதா என்று பார்க்கிறார்கள்:

```text id="k7"
(-1)a + a
```

---

கணக்கிடு:

```text id="k8"
(-1)a + 1a
```

↓

```text id="k9"
(-1+1)a
```

↓

```text id="k10"
0a
```

↓

```text id="k11"
0
```

---

0 வந்துவிட்டது.

அதனால்:

```text id="k12"
(-1)a
```

என்பது

```text id="k13"
a
```

-வின் additive inverse.

---

ஆனால் additive inverse-ன் பெயர் ஏற்கனவே:

```text id="k14"
-a
```

---

அதனால்:

```text id="k15"
(-1)a = -a
```

---

### ஒரு எண் உதாரணம்

```text id="k16"
a=5
```

---

நாம் நிரூபிக்க வேண்டியது:

```text id="k17"
(-1)×5 = -5
```

---

எப்படி check செய்வது?

```text id="k18"
(-1)×5 + 5
```

---

```text id="k19"
-5 + 5
```

---

```text id="k20"
0
```

---

அதனால்:

```text id="k21"
(-1)×5
```

என்பது 5-ன் additive inverse.

---

5-ன் additive inverse யார்?

```text id="k22"
-5
```

---

எனவே:

```text id="k23"
(-1)×5 = -5
```

---

அதனால் proof-ல் இருக்கும்:

```text id="k24"
+a
```

என்பது **கூடுதலாக சேர்த்தது அல்ல**.

மாறாக,

> "`(-1)a` உண்மையில் `-a` தானா?" என்று சோதிக்க பயன்படுத்திய test.

அந்த test:

```text id="k25"
(-1)a + a = 0 ?
```

என்பதுதான். 🎯


-------------------------------------------------

`additive inverse. ஆக இருந்தால் மட்டுமே அது சரி என்று வைத்துக் கொள்ளலாமா?`


ஆம், இந்த சூழ்நிலையில் **அப்படித்தான்**.

புத்தகம் N7-ல்:

```text
(-1)a = -a
```

என்பதை நிரூபிக்க வேண்டியிருந்தது.

---

ஆனால் `-a` என்றால் என்ன?

Definition:

```text
a + (-a) = 0
```

---

அதாவது:

> `a`-வுடன் கூட்டினால் `0` தரும் எண்.

---

எனவே `(-1)a`-வும் `a`-வுடன் கூட்டினால் `0` தருகிறதா என்று பார்க்கிறார்கள்:

```text
(-1)a + a
=
0
```

---

இப்போது ஒரு முக்கியமான Mathematical fact:

ஒரு எண்ணிற்கு **additive inverse ஒன்று மட்டுமே இருக்கும்**.

உதாரணம்:

```text
5
```

க்கு

```text
-5
```

தான் additive inverse.

---

இப்போது வைத்துக்கொள்:

```text
x + 5 = 0
```

மற்றும்

```text
y + 5 = 0
```

---

அப்போது:

```text
x = y
```

ஆகவேண்டும்.

ஏனெனில் 5-ஐ cancel செய்தால் இரண்டும் ஒரே எண்.

---

அதனால்:

```text
(-1)a + a = 0
```

மற்றும்

```text
(-a) + a = 0
```

---

இரண்டும் உண்மை.

அதனால்:

```text
(-1)a
=
-a
```

---

ஆனால் கவனிக்க:

> "Additive inverse ஆக இருந்தால் மட்டுமே இரண்டு எண்களும் சமம்"

என்று பொதுவாக சொல்லக்கூடாது.

சரியான கூற்று:

> **ஒரே எண்ணான `a`-க்கு additive inverse ஆக இருக்கும் இரண்டு expressions இருந்தால், அவை ஒன்றுக்கொன்று சமம்.**

அதாவது:

```text
x + a = 0
```

மற்றும்

```text
y + a = 0
```

என்றால்

```text
x = y
```

---

இதைத்தான் புத்தகம் பயன்படுத்துகிறது.

அதனால் N7 proof-ன் logic:

```text
(-1)a + a = 0
```

↓

"`(-1)a` என்பது `a`-வின் additive inverse"

↓

"`-a` என்பதும் `a`-வின் additive inverse"

↓

ஒரே எண்ணிற்கு additive inverse ஒன்று மட்டுமே

↓

```text
(-1)a = -a
```

🎯 இதுதான் முழு reasoning.


------------------------------------------------

