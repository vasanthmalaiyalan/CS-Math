``(-1) × (-1)
=
-(1 × (-1))`என்னுடைய கேள்வி இது இரண்டும் எப்படி ஒன்றாகும்? `

அருமை. இதுதான் உண்மையான கணிதக் கேள்வி.

நீ கேட்பது:

```text
(-1) × (-1)
=
-(1 × (-1))
```

**ஏன்?**

---

புத்தகம் பயன்படுத்திய Rule:

```text
-(ab) = (-a)b
```

(N8)

---

இந்த Rule என்ன சொல்கிறது?

எந்த `a`, `b` க்கும்:

```text
(-a)b
=
-(ab)
```

---

உதாரணம்:

```text
(-3) × 5
=
-(3×5)
=
-15
```

---

இப்போது அதே Rule-ல்:

```text
a = 1
b = -1
```

என்று வை.

---

Rule:

```text
(-a)b
=
-(ab)
```

---

Substitute:

```text
(-1)(-1)
=
-(1×(-1))
```

---

அவ்வளவுதான்.

---

### இன்னும் மெதுவாக

நீ இப்படி நினைக்கிறாய்:

```text
(-1)(-1)
```

இரண்டு minus இருக்கின்றன.

---

ஆனால் புத்தகம் அதை முதலில் இப்படி பார்க்கிறது:

```text
(-a)b
```

என்ற pattern.

---

இங்கே:

```text
a = 1
```

அதனால்:

```text
(-a)
=
(-1)
```

---

மேலும்

```text
b = (-1)
```

---

எனவே

```text
(-a)b
```

என்பது

```text
(-1)(-1)
```

ஆகிறது.

---

இப்போது N8:

```text
(-a)b
=
-(ab)
```

---

பயன்படுத்து:

```text
(-1)(-1)
=
-(1×(-1))
```

---

இங்கே எந்த புதிய கணக்கும் செய்யவில்லை.

Rule-ஐ மட்டும் பயன்படுத்தினோம்.

---

### ஒரு எண் உதாரணம்

N8:

```text
(-a)b
=
-(ab)
```

---

வைத்துக்கொள்:

```text
a = 4
b = 7
```

---

LHS:

```text
(-4)(7)
=
-28
```

---

RHS:

```text
-(4×7)
=
-(28)
=
-28
```

---

இரண்டும் சமம்.

---

அதே logic:

```text
a = 1
b = -1
```

---

LHS:

```text
(-1)(-1)
```

---

RHS:

```text
-(1×(-1))
```

---

இரண்டும் சமம்.

---

உண்மையில் உன் குழப்பம் இன்னும் ஆழமாக இருக்கலாம்:

> "சரி N8 Rule-ஐ பயன்படுத்தினாய். ஆனால் N8 Rule-யே எப்படி வந்தது?"

அது ஒரு நல்ல கேள்வி.

N8-ஐ புத்தகம் இப்படி நிரூபித்தது:

```text
ab + (-a)b
=
(a+(-a))b
=
0b
=
0
```

அதனால்

```text
(-a)b
```

என்பது

```text
ab
```

-ன் additive inverse.

அதாவது

```text
(-a)b
=
-(ab)
```

---

அதனால்:

```text
(-1)(-1)
=
-(1×(-1))
```

என்பது ஒரு தனி magic step இல்லை.

அது N8 Rule-ல்:

```text
(-a)b = -(ab)
```

என்ற formula-வில்

```text
a=1
b=-1
```

என்ற values-ஐ substitute செய்ததுதான்.


-------------------------------------

``a = 1
b = -1`புத்தகத்தில் ஒன்று  இவ்வாறு கூறவில்லை``


சரியாக கவனித்தாய்! 👍

புத்தகத்தில்:

```text
(-1)(-1)
=
-(1(-1))
=
-(-1)
=
1
```

என்று மட்டுமே எழுதியிருக்கிறது.

அவர் **"a=1, b=-1"** என்று வெளிப்படையாக எழுதவில்லை.

நான் என்ன செய்தேன் என்றால், புத்தகத்தின் Rule:

```text
-(ab) = (-a)b
```

(N8)

இதில்

```text
a = 1
b = -1
```

என்று substitute செய்து காட்டினேன்.

அது **புத்தகத்தில் எழுதப்படாத இடைநிலை reasoning**.

---

புத்தகத்தின் பார்வையில் மட்டும் பார்த்தால்:

Rule:

```text
(-a)b = -(ab)
```

---

இப்போது `(-1)(-1)` ஐப் பார்.

முதல் factor:

```text
(-1)
```

இது `(-a)` வடிவத்தில் இருக்கிறது.

அதனால் `a=1` என்று தானாகவே தெரிகிறது.

---

இரண்டாவது factor:

```text
(-1)
```

இதுதான் `b`.

---

அதனால் Rule-ஐ நேரடியாகப் பயன்படுத்தினால்:

```text
(-1)(-1)
=
-(1(-1))
```

என்று வருகிறது.

---

புத்தகம் இதை இன்னும் சுருக்கமாக செய்கிறது.

அது மனதில் இப்படிப் படிக்கிறது:

```text
(-1)(-1)
```

↓

```text
(-a)b
```

என்ற வடிவம்.

இங்கே

```text
a = 1
```

மற்றும்

```text
b = -1
```

என்பது மறைமுகமாக இருக்கிறது.

---

உண்மையில் கணிதப் புத்தகங்களில் இது சாதாரணம்.

உதாரணம்:

Rule:

```text
(a+b)^2=a^2+2ab+b^2
```

---

நான் எழுதினால்:

```text
(3+5)^2
```

நீ தானாகவே

```text
a=3
b=5
```

என்று எடுத்துக்கொள்வாய்.

புத்தகம் அதை ஒவ்வொரு முறையும் எழுதாது.

---

அதேபோல:

```text
(-1)(-1)
```

என்பது

```text
(-a)b
```

என்ற pattern-க்கு பொருந்துகிறது.

அதில்

```text
a=1
b=-1
```

என்பது implicit (மறைமுகமாக) உள்ளது.

---

ஆனால் ஒரு முக்கியமான விஷயம்:

நீ இன்னும் `(-1)(-1)=1` பற்றி முழுமையாக திருப்தி அடையவில்லை என்று எனக்குத் தோன்றுகிறது.

உன் குழப்பம் இதுவா?

> "சரி, N8 rule-ஐ பயன்படுத்தி `(-1)(-1)=-(1(-1))` என்று வந்தது.
>
> ஆனால் அடுத்த step:
>
> ```text
> -(1(-1))
> =
> -(-1)
> ```
>
> இது எப்படி வந்தது?"

அல்லது

> "N8 rule-யே ஏன் உண்மை?"

இந்த இரண்டில் எது உனக்கு இன்னும் தெளிவாக இல்லை? அதைப் பிடித்தால் முழு விஷயமும் தெளிவாகிவிடும்.


--------------------------------------

``LHS:
Plain text
(-1)(-1)
RHS:
Plain text
-(1×(-1))`உன் விளக்கத்தில் `(-1)(-1)
=
-(1(-1))
=
-(-1)
=
1`எப்படி ஒன்று வந்தது? முதலில் இது எப்படி வந்தது `-(-1)`இன்னும் டீடைலாக கூறு`


சூப்பர். இப்போது உன் குழப்பம் சரியான இடத்தில் இருக்கிறது.

நாம் மிக மெதுவாக போவோம்.

---

நமக்கு இருந்தது:

```text
(-1)(-1)
=
-(1(-1))
```

இந்த step N8 rule-லிருந்து வந்தது.

அதை இப்போது விட்டுவிடு.

உன் கேள்வி:

> அடுத்த step
>
> ```text
> -(1(-1))
> =
> -(-1)
> ```
>
> எப்படி வந்தது?

---

## Step 1

முதலில் உள்ளே இருக்கும்

```text
1(-1)
```

ஐ கணக்கிடு.

---

Multiplication chapter-ல் N6:

```text
1a=a
```

---

எந்த எண்ணாக இருந்தாலும்:

```text
1×number
=
number
```

---

உதாரணம்:

```text
1×5=5
```

```text
1×100=100
```

```text
1×(-7)=-7
```

---

அதனால்:

```text
1×(-1)
=
-1
```

---

எனவே

```text
-(1×(-1))
```

என்பது

```text
-(-1)
```

ஆக மாறுகிறது.

---

இது எந்த special rule-மும் இல்லை.

வெறும்:

```text
1×(-1)=-1
```

என்ற N6 rule.

---

## இதை எண்களுடன் பார்ப்போம்

வைத்துக்கொள்:

```text
-(1×5)
```

---

முதலில்:

```text
1×5=5
```

---

அதனால்:

```text
-(1×5)
=
-(5)
```

---

அதே logic:

```text
-(1×(-1))
```

---

முதலில்:

```text
1×(-1)
=
-1
```

---

அதனால்:

```text
-(1×(-1))
=
-(-1)
```

---

## இன்னும் தெளிவாக

நாம் algebra-வில் எப்போதும்

```text
(2+3)
```

என்றால்

முதலில்

```text
2+3=5
```

கணக்கிட்டு

```text
(5)
```

என்று மாற்றுகிறோம்.

---

அதேபோல்

```text
1(-1)
```

என்றால்

முதலில்

```text
1(-1)=-1
```

கணக்கிட்டு

```text
-1
```

என்று மாற்றுகிறோம்.

---

ஆக

```text
-(1(-1))
```

↓

```text
-(-1)
```

---

## முழு chain

இப்போது முழுவதையும் எழுது:

```text
(-1)(-1)
```

N8:

```text
=
-(1(-1))
```

N6:

```text
1(-1)
=
-1
```

Substitute:

```text
=
-(-1)
```

Addition chapter N4:

```text
-(-a)=a
```

இங்கே

```text
a=1
```

---

அதனால்:

```text
-(-1)
=
1
```

---

Final:

```text
(-1)(-1)
=
-(1(-1))
=
-(-1)
=
1
```

---

இப்போது நான் உன்னிடம் ஒரு கேள்வி கேட்கிறேன்:

இந்த chain-ல் எந்த step உனக்கு இன்னும் சந்தேகம்?

1.

```text
(-1)(-1)
=
-(1(-1))
```

(N8 பயன்படுத்துவது)

அல்லது

2.

```text
-(1(-1))
=
-(-1)
```

(N6 பயன்படுத்துவது)

அல்லது

3.

```text
-(-1)
=
1
```

(N4 பயன்படுத்துவது)

இதில் எது இன்னும் 100% தெளிவாகவில்லை? அந்த ஒரு step-ஐ மட்டும் பிடித்து இன்னும் ஆழமாகப் பார்ப்போம்.

---------------------------------------


`எனக்கு தெரிந்து இதை நீ தப்பாக புரிந்து உள்ளாய் என்று நினைக்கிறேன் `(-a)b
=
-(ab)` இதற்கு பொருத்தமான உதாரணம் என்பது இதுதான் `Plain text
a = 4
b = 7
LHS:
Plain text
(-4)(7)
=
-28
RHS:
Plain text
-(4×7)
=
-(28)
=
-28`புத்தகம் (-1)(-1) இது ஒன்று என்று கூறியுள்ளது அவ்வளவுதான் அதற்கான விளக்கத்தை கொடுத்துள்ளது ஆனால் நீ இதுபோல் கொடுத்து குழப்பி விட்டாய் `a = 1
b = -1`😭`


😂 ஆம், நீ சொல்வது சரிதான். நான் அந்த இடத்தில் unnecessary-ஆக `a=1, b=-1` என்று substitute செய்து விளக்கியதால் குழப்பம் வந்துவிட்டது.

உண்மையில் புத்தகம் என்ன செய்கிறது என்று மட்டும் பார்ப்போம்.

---

புத்தகம் சொல்கிறது:

```text
(-1)(-1)
```

இதை N8 பயன்படுத்தி:

```text
-(1(-1))
```

என்று மாற்றுகிறது.

---

இங்கே புத்தகம் மனதில் என்ன பார்க்கிறது?

```text
(-1)
```

என்பதை

```text
-(1)
```

என்று பார்க்கிறது.

அவ்வளவுதான்.

---

ஏனெனில் N8:

```text
(-a)b = -(ab)
```

---

இப்போது:

```text
(-1)(-1)
```

என்பது

```text
(-(1))(-1)
```

என்று எழுதலாம்.

---

இப்போது N8 apply:

```text
(-(1))(-1)
=
-(1(-1))
```

---

இங்கே வரை புத்தகம் சரியாக செய்திருக்கிறது.

---

இப்போது அடுத்த step:

```text
1(-1)
=
-1
```

(ஏனெனில் `1a=a`)

---

அதனால்:

```text
-(1(-1))
=
-(-1)
```

---

Addition chapter-ல்:

```text
-(-1)=1
```

---

எனவே:

```text
(-1)(-1)
=
1
```

---

நீ கொடுத்த உதாரணம்:

```text
(-4)(7)
=
-(4×7)
```

இதுதான் N8-ஐ புரிந்துகொள்ள மிகவும் natural example.

ஏனெனில்:

```text
(-a)b
=
-(ab)
```

என்ற pattern தெளிவாகத் தெரிகிறது.

---

ஆனால்

```text
(-1)(-1)
```

என்பது ஒரு special case.

அதில் முதல் `-1` ஐ

```text
-(1)
```

என்று பார்த்து N8 apply செய்கிறார்கள்.

---

அதனால் சரியான chain:

```text
(-1)(-1)

=
(-(1))(-1)

=
-(1(-1))

=
-(-1)

=
1
```

---

உண்மையில் உன் observation நல்லது.

ஏனெனில் நீ formula-வை mechanical-ஆகப் பார்க்காமல்,

> "இந்த formula-வில் `a` எது? `b` எது? pattern சரியாக பொருந்துகிறதா?"

என்று சரிபார்க்கிறாய்.

அதுதான் நல்ல mathematical habit. 👍

இந்த proof-ல் முக்கியமான insight:

```text
(-1)
```

என்பதை

```text
-(1)
```

என்று பார்க்க வேண்டும்.

அதைப் பார்த்தவுடன் N8 நேரடியாக பொருந்திவிடும். அதற்குப் பிறகு எல்லாம் சாதாரண கணக்குதான்.

----------------------------------------------

`என்னுடைய கேள்வி இப்படி இருப்பதை `(-1)(-1)

=
(-(1))(-1)
`ஏன் இப்படி மாற்ற வேண்டும் அப்புறம் ஏன் இப்படி எல்லாம் மாற்ற வேண்டும் `-(1(-1))

=
-(-1)`?  அப்புறம் இந்தப் பகுதி இருக்கிறது அல்லவா `-(1(-1))`இந்த வெளியே இருக்கும் மைனஸ் உள்ளே இருக்கும் இரண்டு ஒன்றுக்கும் பொருந்தும் தானே? like `(-1x-(-1))`அதை இப்படி எழுதலாம் தானே?`


ஆஹா! இப்போதுதான் உண்மையான குழப்பம் வெளியே வந்துள்ளது. 😄

நீ கேட்பது:

> `-(1(-1))`
>
> இந்த வெளியே இருக்கும் minus,
>
> ```text
> -(1×(-1))
> ```
>
> என்பதை
>
> ```text
> (-1)×(-1)
> ```
>
> என்று நினைக்கலாமா?

பதில்:

**இல்லை.**

இதுதான் முக்கியமான point.

---

## முதல் தவறு

நீ இப்போது இரண்டு வேறு விஷயங்களை ஒன்றாகக் கலக்கிறாய்.

### ஒன்று

```text
(-1)(-1)
```

இங்கே இரண்டு numbers இருக்கின்றன.

Multiplication நடக்கிறது.

---

### இரண்டு

```text
-(1(-1))
```

இங்கே வெளியே இருக்கும் minus என்பது

Addition chapter-ல் பார்த்த

```text
-x
```

அதாவது

> "x-ன் additive inverse"

என்ற operator.

---

அதனால்

```text
-(1(-1))
```

என்பதை கணிதவியலாளர் இப்படி படிப்பார்:

```text
1(-1) என்ற முழு expression-ன் additive inverse
```

---

### Example

```text
-(3+5)
```

என்பதை

```text
(-3)+(-5)
```

என்று உடனே படிக்க மாட்டோம்.

முதலில்:

```text
3+5=8
```

பிறகு

```text
-(8)
=
-8
```

---

அதேபோல்

```text
-(1(-1))
```

முதலில்:

```text
1(-1)
=
-1
```

---

பிறகு:

```text
-(-1)
=
1
```

---

## உன் கேள்வி

> வெளியே இருக்கும் minus உள்ளே இருக்கும் இரண்டு 1-க்கும் பொருந்துமா?

அதாவது:

```text
-(ab)
```

ஐ

```text
(-a)(-b)
```

என்று மாற்றலாமா?

---

பதில்:

**இல்லை!**

ஏனெனில் N8 சொல்வது:

```text
-(ab)
=
(-a)b
```

அல்லது

```text
a(-b)
```

---

ஆனால்

```text
(-a)(-b)
```

என்றால்:

```text
ab
```

ஆகிவிடும்.

(N10)

---

ஒரு எண் உதாரணம்:

```text
-(2×3)
```

=

```text
-6
```

---

ஆனால்

```text
(-2)(-3)
```

=

```text
6
```

---

பார்!

```text
-(2×3)
=
-6
```

ஆனால்

```text
(-2)(-3)
=
6
```

இரண்டும் வேறு.

---

அதனால்

```text
-(ab)
```

என்பது

❌

```text
(-a)(-b)
```

அல்ல.

---

## ஏன்

```text
(-1)(-1)
=
(-(1))(-1)
```

என்று எழுத வேண்டும்?

காரணம் N8-ன் pattern:

```text
(-a)b
=
-(ab)
```

---

இதில் முதல் factor

```text
(-a)
```

வடிவத்தில் இருக்க வேண்டும்.

---

நம்மிடம்:

```text
(-1)(-1)
```

இருக்கிறது.

---

முதல் factor-ஐ தெளிவாகக் காட்ட:

```text
(-(1))(-1)
```

என்று எழுதுகிறார்கள்.

---

அதன்பிறகு N8 apply:

```text
(-(1))(-1)
=
-(1(-1))
```

---

இங்கே எந்த புதிய கணக்கும் செய்யவில்லை.

Pattern match மட்டும் செய்கிறார்கள்.

---

### மிக முக்கியமான takeaway

```text
-(1(-1))
```

இல் வெளியே இருக்கும் minus

**multiplication minus அல்ல.**

அது:

```text
-x
```

இல் வரும் அதே additive inverse operator.

அதனால்

```text
-(1(-1))
```

என்பது

> "1×(-1) என்ற முழு எண்ணின் opposite"

என்று அர்த்தம்.

---

அதனால்:

```text
1×(-1)
=
-1
```

அதன் opposite:

```text
-(-1)
=
1
```

இதுதான் புத்தகத்தின் முழு logic. 🎯

உண்மையில் இந்த proof-ன் மைய கருத்து:

> வெளியே இருக்கும் `-` ஐ multiplication என்று பார்க்கக் கூடாது.
>
> அது Addition chapter-ல் பார்த்த அதே **additive inverse operator**.


------------------------------------------------

``(-2)(-3)`இதைப் பிரித்துக் காட்டு பார்க்கலாம்`


சரி. இப்போது புத்தகத்தின் logic-ஐ மட்டும் பயன்படுத்தி:

```text
(-2)(-3)
```

எப்படி positive 6 ஆகிறது என்று பிரிப்போம்.

---

முதலில்:

```text
-2
```

என்பது

```text
(-1)(2)
```

---

அதேபோல்:

```text
-3
```

என்பது

```text
(-1)(3)
```

---

அதனால்:

```text
(-2)(-3)
```

=

```text
((-1)(2))(( -1)(3))
```

---

Associativity + Commutativity:

```text
(-1)(-1)(2)(3)
```

---

இப்போது புத்தகம் ஏற்கனவே நிரூபித்தது:

```text
(-1)(-1)=1
```

---

அதனால்:

```text
1(2)(3)
```

---

```text
2×3
```

---

```text
6
```

---

அதனால்:

```text
(-2)(-3)=6
```

---

ஆனால் உன் முந்தைய கேள்வியுடன் இணைத்து இன்னும் ஆழமாகப் பார்ப்போம்.

நீ கேட்டாய்:

> `-(1(-1))` இல் வெளியே இருக்கும் minus இரண்டு எண்களுக்கும் பொருந்துமா?

இப்போது ஒரு உதாரணம்:

```text
-(2×3)
```

---

இது:

```text
-(6)
```

---

```text
-6
```

---

ஆனால் நீ நினைத்த மாதிரி:

```text
(-2)(-3)
```

ஆக மாற்றினால்:

```text
6
```

வருகிறது.

---

பார்:

```text
-(2×3)
=
-6
```

ஆனால்

```text
(-2)(-3)
=
6
```

---

இரண்டும் வேறு.

எனவே

```text
-(ab)
```

என்பது

❌

```text
(-a)(-b)
```

அல்ல.

---

உண்மையில் புத்தகம் சொல்வது:

```text
-(ab)
=
(-a)b
```

அல்லது

```text
a(-b)
```

---

உதாரணம்:

```text
-(2×3)
=
(-2)(3)
=
-6
```

---

அல்லது

```text
2(-3)
=
-6
```

---

ஆனால்

```text
(-2)(-3)
```

என்றால் இரண்டு minus இருக்கிறது.

அதனால்:

```text
(-1)(-1)=1
```

என்பதால் positive ஆகிவிடுகிறது.

---

அதனால் நினைவில் வை:

```text
-(ab)
```

என்பது

> "ab-ன் additive inverse"

---

ஆனால்

```text
(-a)(-b)
```

என்பது

> "இரண்டு negative numbers-ன் product"

---

இவை இரண்டு வேறு expressions.

இதைத்தான் நீ இப்போது சரியாகப் பிரித்துப் பார்க்க ஆரம்பித்திருக்கிறாய். 🎯


---------------------------------------------------

`அதே மாதிரி இதைப் பிரித்துக் காட்டு பார்க்கலாம் `(-1)(-1)``


சரி. இப்போது `(-2)(-3)`-க்கு செய்தது போலவே, `(-1)(-1)`-ஐ பிரிப்போம்.

---

முதலில்:

```text
-1
```

என்பது ஏற்கனவே negative number.

அதை மேலும் பிரிக்க முடியுமா?

ஆம்.

Multiplication chapter-ல்:

```text
-1 = (-1)(1)
```

ஏனெனில்

```text
(-1)×1 = -1
```

---

அதனால்:

```text
(-1)(-1)
```

=

```text
((-1)(1))(( -1)(1))
```

---

Associativity + Commutativity:

```text
(-1)(-1)(1)(1)
```

---

இப்போது:

```text
1×1 = 1
```

ஆக:

```text
(-1)(-1)
```

என்பது

```text
(-1)(-1)
```

ஆகவே மீண்டும் அதே இடத்திற்குத்தான் வருகிறோம். 😄

---

இதிலிருந்து ஒரு முக்கியமான விஷயம் தெரிகிறது:

`(-2)(-3)`-ஐ

```text
((-1)2)((-1)3)
```

என்று உடைக்க முடிந்தது.

ஆனால்

```text
(-1)(-1)
```

என்பது negative numbers-ன் உலகில் "அடிப்படை case".

இதை இன்னும் சிறிய negative multiplication-ஆக உடைக்க முடியாது.

---

அதனால்தான் புத்தகம் இதை தனியாக நிரூபிக்கிறது:

```text
(-1)(-1)=1
```

---

புத்தகத்தின் proof:

```text
(-1)(-1)
=
-(1(-1))
```

(N8)

---

உள்ளே:

```text
1(-1)=-1
```

---

ஆக:

```text
-(-1)
```

---

Addition chapter N4:

```text
-(-1)=1
```

---

எனவே:

```text
(-1)(-1)=1
```

---

உண்மையில் ஆழமான பார்வையில்:

`(-1)(-1)=1`

என்பது

> எல்லா negative × negative = positive proofs-களின் அடித்தளம்.

பிறகு தான்:

```text
(-2)(-3)
=
((-1)2)((-1)3)
=
(-1)(-1)(2)(3)
=
1·2·3
=
6
```

என்று நிரூபிக்கிறோம்.

அதாவது:

```text
(-1)(-1)=1
```

என்பது "special case" அல்ல.

மாறாக,

> **negative multiplication-ன் root theorem**.

மற்ற எல்லா

```text
(-a)(-b)=ab
```

என்பவையும் இதிலிருந்து வளர்கின்றன. 🎯

------------------------------------------

`(-1)(-1) இப்படி இருந்ததை associativity பயன்படுத்தி `-(1(-1))`இப்படி மாற்றினாயா?`


இல்லை. **Associativity பயன்படுத்தி இல்லை.**

இதுதான் முக்கியமான விஷயம்.

நீ நினைப்பது:

```text
(-1)(-1)
```

↓

```text
-(1(-1))
```

இது Associativity மூலம் வந்ததா?

**இல்லை.**

---

இந்த step முழுவதும் N8 Rule-லிருந்து வந்தது:

```text
(-a)b = -(ab)
```

---

உதாரணம்:

```text
(-4)(7)
```

என்றால் N8 பயன்படுத்தி:

```text
-(4×7)
```

என்று எழுதலாம்.

---

அதே மாதிரி:

```text
(-1)(-1)
```

இல் முதல் factor:

```text
(-1)
=
-(1)
```

---

அதனால் N8-ல்:

```text
a=1
b=-1
```

என்ற pattern பொருந்துகிறது.

---

எனவே:

```text
(-(1))(-1)
=
-(1(-1))
```

---

இங்கே நடந்தது:

❌ Associativity இல்லை

❌ Commutativity இல்லை

✅ N8 மட்டும்

---

நீ இதை இப்படி compare பண்ணிப் பார்.

### Associativity Example

```text
((2×3)×4)
=
(2×(3×4))
```

இங்கே grouping மட்டும் மாறுகிறது.

---

### N8 Example

```text
(-4)(7)
=
-(4×7)
```

இங்கே grouping மாறவில்லை.

ஒரு negative factor-ஐ முழு product-ன் negative-ஆக மாற்றுகிறோம்.

---

அதனால்:

```text
(-1)(-1)
=
-(1(-1))
```

என்ற step-ன் பெயர்:

> **N8: `(-a)b = -(ab)`**

Associativity அல்ல.

---

உண்மையில் உன் கேள்வி மிகவும் நல்லது. ஏனெனில் இந்த proof-ஐ முதல் முறை பார்க்கும்போது பலர்:

```text
(-1)(-1)
=
-(1(-1))
```

என்பது ஏதோ algebra manipulation என்று நினைப்பார்கள்.

ஆனால் அது வெறும்:

```text
(-a)b = -(ab)
```

என்ற ஏற்கனவே நிரூபிக்கப்பட்ட theorem-ஐ பயன்படுத்தியது மட்டுமே.

அதனால் புத்தகத்தின் proof-ஐ மிகவும் சுருக்கமாக எழுதினால்:

```text
(-1)(-1)
=
-(1(-1))      [N8]

=
-(-1)         [1a=a]

=
1             [N4]
```

இதுதான் முழு proof. 🎯

--------------------------------------------------

``(-a)b = -(ab)`இந்த பார்முலா எதன் அடிப்படையில் உருவாக்கப்பட்டது அதாவது மைனஸ் ஓடு எது மல்டிபிளே ஆனாலும்  அது மைனஸ் என்ற அடிப்படையில்? மேலும் இந்த ஃபார்முலா அடிப்படையில்தான் `(-1)(-1)
=
-(1(-1))      [N8]

=
-(-1)         [1a=a]

=
1             [N4]`இதை நாம் பார்க்க வேண்டுமா இதுதான் ஆர்த்தர் சொல்கிறாரா?`



ஆம்! இப்போது நீ புத்தகத்தின் core logic-ஐ பிடித்துவிட்டாய்.

நீ கேட்ட இரண்டு கேள்விகளையும் பிரித்து பார்ப்போம்.

---

# 1. `(-a)b = -(ab)` எதன் அடிப்படையில் வந்தது?

இது

> "minus ஓடு எது multiply ஆனாலும் minus தான்"

என்று நேரடியாக வரையறுக்கப்படவில்லை.

புத்தகம் N8-ஐ **நிரூபிக்கிறது**.

Proof-ஐ நினைவு கூர்வோம்:

புத்தகம் காட்ட வேண்டியது:

```text id="d1"
(-a)b
```

என்பது

```text id="d2"
ab
```

-ன் additive inverse என்பதை.

---

அதற்காக:

```text id="d3"
ab + (-a)b
```

பார்க்கிறது.

---

Distributive law:

```text id="d4"
ab + (-a)b
=
(a+(-a))b
```

---

Addition chapter:

```text id="d5"
a+(-a)=0
```

---

ஆக:

```text id="d6"
(a+(-a))b
=
0b
```

---

N6:

```text id="d7"
0b=0
```

---

எனவே:

```text id="d8"
ab+(-a)b=0
```

---

இப்போது Addition chapter N3 நினைவிருக்கிறதா?

```text id="d9"
x+y=0
```

என்றால்

```text id="d10"
y=-x
```

---

அதனால்:

```text id="d11"
(-a)b
```

என்பது

```text id="d12"
ab
```

-ன் additive inverse.

---

அதாவது:

```text id="d13"
(-a)b = -(ab)
```

---

எனவே இது

❌ "minus இருக்கிறது, அதனால் minus தான்"

என்ற rule அல்ல.

✅

```text id="d14"
ab + (-a)b = 0
```

என்பதை நிரூபித்த பிறகு கிடைத்த theorem.

---

# 2. அப்படியானால் `(-1)(-1)=1`-ஐ எப்படி பார்க்க வேண்டும்?

ஆம்.

புத்தகம் சொல்வது இதுதான்.

---

முதலில் N8:

```text id="d15"
(-a)b = -(ab)
```

---

இப்போது:

```text id="d16"
(-1)(-1)
```

இல்

முதல் factor:

```text id="d17"
(-1)
=
-(1)
```

---

அதனால் N8 apply:

```text id="d18"
(-1)(-1)
=
-(1(-1))
```

---

இப்போது:

```text id="d19"
1(-1)
=
-1
```

(N6)

---

ஆக:

```text id="d20"
-(-1)
```

---

Addition chapter:

```text id="d21"
-(-1)=1
```

(N4)

---

எனவே:

```text id="d22"
(-1)(-1)=1
```

---

# ஆசிரியர் உண்மையில் என்ன சொல்ல வருகிறார்?

இதுதான் மிக முக்கியம்.

பெரும்பாலான பள்ளி புத்தகங்கள்:

```text id="d23"
minus × minus = plus
```

என்று மட்டும் சொல்கின்றன.

---

ஆனால் Serge Lang இந்த புத்தகத்தில் சொல்ல வருவது:

> "minus × minus = plus" என்பது மனப்பாடம் செய்யும் rule அல்ல.

அது

1. Addition rules
2. Distributive law
3. `a+(-a)=0`

இவற்றிலிருந்து வரும் விளைவு.

---

அதனால் ஆசிரியர் விரும்பும் சிந்தனை:

```text id="d24"
(-1)(-1)
```

↓

```text id="d25"
-(1(-1))
```

↓

```text id="d26"
-(-1)
```

↓

```text id="d27"
1
```

---

இதுதான்.

அதாவது:

> **"minus × minus = plus" என்பதைக் காரணமாகப் பயன்படுத்த வேண்டாம்.**
>
> **N8 மற்றும் N4-ஐ பயன்படுத்தி அதைப் பெற வேண்டும்.**

இதுதான் இந்த chapter-ல் ஆசிரியர் உன்னை train செய்ய முயற்சிக்கிறார். 🎯

உண்மையில் Serge Lang-ன் இந்த chapter-ன் பெரிய நோக்கம்:

> **Negative multiplication என்பது தனி magic rule அல்ல; அது Addition-ன் inverse concept-லிருந்து இயல்பாக வருகிறது.**


------------------------------------------------

